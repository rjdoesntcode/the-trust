# SHA-512 of every file under /canonical/. Generated by scripts/hash.sh.
# Verify: sha512sum -c HASHES.txt        (a second, independent manifest is HASHES.sha3-512.txt)
# Entries are only ever appended. A changed digest for an existing path fails the build.
