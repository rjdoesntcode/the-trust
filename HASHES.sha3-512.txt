# SHA3-512 of every file under /canonical/. Generated by scripts/hash.sh.
# Verify: while read h p; do [ "${h#\#}" = "$h" ] && [ "$(openssl dgst -sha3-512 -r "$p" | cut -d' ' -f1)" = "$h" ] && echo "ok $p"; done < HASHES.sha3-512.txt
# Entries are only ever appended. A changed digest for an existing path fails the build.
